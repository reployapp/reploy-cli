{
  "name": "reploy",
  "version": "0.1.0",
  "description": "Update and manage your React Native application versions",
  "author": "Joshua Sierles",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf ./lib && babel -d lib/ src/ && chmod 755 lib/*",
    "watch": "babel -d lib/ src/ --watch",
    "pre-publish": "npm run build"
  },
  "dependencies": {
    "axios": "^0.5.4",
    "bluebird": "^2.9.30",
    "commander": "2.8.1",
    "home-config": "^0.1.0",
    "plist": "^1.1.0",
    "superagent": "^1.2.0",
    "timeago": "^0.2.0"
  },
  "devDependencies": {
    "babel": "^5.6.14"
  },
  "bin": {
    "reploy": "lib/index.js"
  }
}
